import {Component} from '@angular/core';
@Component({
    selector: 'app-server', //determines html tag, must be unique
    templateUrl: './server.component.html' //must be relative path for webpack to use
})
export class ServerComponent {

}